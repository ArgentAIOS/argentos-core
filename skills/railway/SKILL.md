---
name: railway
description: "Use the railway_deploy core tool to test Railway API access, list projects, and run custom GraphQL operations via Railway API token."
---

# Railway Skill

Use `railway_deploy` for Railway operations.

Primary actions:

- `test_connection`
- `list_projects`
- `graphql`

Required key: `RAILWAY_API_TOKEN`.
