# aos-1password agent harness

Python Click wrapper for the `aos-1password` connector.

```bash
python -m pip install -e .
aos-1password --json capabilities
aos-1password --json health
aos-1password --json config show
```
