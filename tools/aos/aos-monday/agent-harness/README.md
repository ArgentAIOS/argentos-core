# aos-monday agent harness

Python Click wrapper for the `aos-monday` connector.

The harness provides truthful setup, health, config, and doctor commands plus
live reads for account, workspace, board, item, and update surfaces.

Write commands are exposed for parity with the manifest, but they remain
scaffolded only.
