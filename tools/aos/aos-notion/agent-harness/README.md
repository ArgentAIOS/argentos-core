# aos-notion agent harness

Python Click wrapper for the `aos-notion` connector.

The harness exposes live read commands for Notion databases, pages, blocks, and
search, and it now executes live write commands for `page.create`,
`page.update`, and `block.append`.
