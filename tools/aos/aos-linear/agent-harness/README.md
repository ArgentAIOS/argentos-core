# aos-linear agent harness

Python Click harness for the Linear connector.

Install locally:

```bash
cd tools/aos/aos-linear/agent-harness
python3 -m venv .venv
source .venv/bin/activate
pip install -e '.[dev]'
aos-linear --json capabilities
```
