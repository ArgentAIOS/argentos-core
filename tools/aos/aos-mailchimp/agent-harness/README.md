# aos-mailchimp agent harness

Python Click wrapper for the `aos-mailchimp` connector.

The harness exposes live read commands for Mailchimp audiences, members, campaigns,
and reports, while keeping write commands scaffolded until the write bridge exists.
