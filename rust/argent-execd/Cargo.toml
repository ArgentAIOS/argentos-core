[package]
name = "argent-execd"
version = "0.1.0"
edition = "2021"
description = "ArgentOS Rust executive shadow daemon scaffold"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
